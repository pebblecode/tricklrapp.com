=form_for @status do |f|
  =render :partial => '/partials/error_messages', :locals => { :object => @status }
  = field_set_tag "Hey! We're ready to queue your tweet", :class => 'input-form' do
    %div.input
      =f.text_area :status, :class => "xxlarge"
      %span.help-block
        Some help text here
    %section.actions
      %button.btn.primary
        Trickle it!
    

