=render :partial => 'form'
  
%ul.tab-headings
  %li{:class => "first #{(@statuses_type == :unpublished) ? "active" : ""}"}
    =link_to "Queue", root_path
  %li{:class => "first #{(@statuses_type == :published) ? "active" : ""}"}
    =link_to "Published", published_path
  
/ %section.status-lists

-if @statuses.present?
  %ul#queued_statuses.status-list
    =render @statuses

